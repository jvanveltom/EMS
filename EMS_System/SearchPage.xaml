<Window x:Class="EMS_System.SearchPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EMS_System"
        mc:Ignorable="d"
        Title="SearchPage" Height="600" Width="1000" ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ImagesDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <local:SearchControl Margin="-6,41,121,-36"></local:SearchControl>
        <local:MenuControl Margin="853,43,-641,419"></local:MenuControl>
        <TextBox x:Name="txt_searchInput" HorizontalAlignment="Left" Height="23" Margin="352,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="212"/>
        <Button Height="23" VerticalAlignment="Center" Command="{Binding SetSelectedItemCommand}" Margin="569,10,398,536">
            <Button.Content>
                <Image Source="{StaticResource SearchBlack}" Height="23" HorizontalAlignment="Center"/>
            </Button.Content>
        </Button>
    </Grid>
</Window>